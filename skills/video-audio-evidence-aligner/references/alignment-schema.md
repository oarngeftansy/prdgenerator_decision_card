# Audio-Visual Alignment Schema

Audio segment: start, end, speaker, transcript, confidence, language.

Subtitle/UI text: timestamp, text, bounding box, OCR confidence, frame ID.

Sound cue: timestamp range, category, confidence, linked event.

Alignment: audio evidence ID, visual evidence IDs, relationship (`supports`, `contradicts`, `adds-context`, `unresolved`), notes.
