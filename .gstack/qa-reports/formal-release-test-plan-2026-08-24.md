# 正式发布回归测试计划

- 正式地址：`http://192.168.50.210:8010/`
- 桌面范围：1366×768、1440×900、1920×1080；不验收移动端
- 项目样本：一路狂飙 `4180cd72eeaa4819be41db50bb4c5011`、角色创建 `d50b2b851a9f4da68275821b39667d80`
- 当前待部署版本：Git `72766f81490407ffbfe88e2e8a23ce67346630ce`、SVN `r93`
- 证据要求：每个交互用例保留操作前/后截图；每页检查控制台错误与失败请求；失败需复现两次。

## 发布门禁用例

| ID | 场景 | 操作 | 通过标准 | 截图证据 |
|---|---|---|---|---|
| TC-01 | 发布版本一致 | 打开正式首页并强制刷新 | `backend=ux90`、`gameplay=gameplay24`，首页 HTTP 200 | 首页与资源版本 |
| TC-02 | 全项目图片完整性 | 遍历两个正式任务全部图片 URL | 角色创建 4/4、一路狂飙 30/30 均为 200；跨项目陈旧引用为 0 | 请求审计结果 |
| TC-03 | P4 参考画面显示 | 分别打开两个项目 P4，滚动全部参考画面 | 无“画面加载失败”；所有可见图 `naturalWidth>0`；竖图保持竖版、无正方形裁切 | 两项目 P4 全页截图 |
| TC-04 | P4 编辑规则 | 点击“编辑规则” | 外层补充区和内层规则区同时展开；编辑器可见且获得焦点；无规则内容变更 | 点击前/后截图 |
| TC-05 | P1 玩法目录 | 两项目点击 P1、来回切换章节 | 页面完成加载；目录项可点击；无无限加载、无 4xx/5xx | 两项目 P1 截图 |
| TC-06 | P2 交互审核保存 | 保存未改变内容并刷新 | 保存成功；不出现“当前环节保存失败”；刷新后状态一致 | 保存前/成功状态/刷新后 |
| TC-07 | P1–P7 导航 | 依次点击顶部七步，再倒序返回 | 每个按钮可点击、目标页正确、无死按钮或跳转失败 | 各步骤关键截图 |
| TC-08 | 折叠与编辑控件族 | 检查规则、证据、参数、问题、补充信息等折叠入口 | 点击后目标内容可见；所有祖先容器同步展开；再次点击行为符合文案 | 控件前/后截图 |
| TC-09 | 新旧项目隔离 | 对比两个任务返回模型中的 `jobId` 与 `/artifacts/{jobId}` | 每个任务只引用自身资源；复制/迁移模型会按 `frameId` 重绑定当前任务 | 数据审计结果 |
| TC-10 | 桌面布局 | 1366、1440、1920 三档检查 P1–P7 | 无文字重叠、面板遮挡、异常横向溢出；主要操作在可见区 | 三档关键截图 |
| TC-11 | 浏览器健康 | 每次交互后读取 console 与 network | 未捕获 JS 错误为 0；业务请求 4xx/5xx 为 0；图片失败为 0 | 控制台与失败请求摘要 |
| TC-12 | 刷新与返回 | P1/P2/P4 操作后刷新、浏览器前进后退 | 当前项目和当前步骤不串项目；允许持久化的 UI 状态正确恢复 | 刷新前/后截图 |

## 当前基线发现

- 角色创建：4 个图片 URL，跨项目引用 0，失败 0。
- 一路狂飙：30 个图片 URL，其中 15 个 P4 引用错误指向旧任务 `8312a91c89e144e6a59f81b982f14c06`，15 个请求均为 404。
- 修复回归：玩法模型与审核 API测试 54/54 通过；前端测试 466/466 通过。
- 正式站仍为旧资源 `ux89/gameplay23`，最终门禁必须在部署后重跑，不能用本地结果代替。
