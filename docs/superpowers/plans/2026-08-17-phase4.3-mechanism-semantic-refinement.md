# Phase 4.3 Mechanism Semantic Refinement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correct MechanismRole assignments and produce six editorially organized, fully traceable samples.

**Architecture:** Extend the deterministic composer with a layered Role Resolver and resolution status. Extend the block renderer with evidence-driven carrier selection and generate separate final prose and role-audit artifacts.

**Tech Stack:** Python, pytest, JSON, Markdown.

## Global Constraints

- No new Rule or closed Gap.
- Predicate patterns only disambiguate structured routing.
- Unresolved dependencies never enter confirmed prose.
- Raw GVE16 content remains unavailable at runtime.

---

### Task 1: Layered Role Resolver

**Files:** `backend/mechanism_composer.py`, `tests/test_mechanism_composer.py`

- [ ] Add failing tests for input constraints, target selection, attack processing, resolution status, and reasons.
- [ ] Run the tests and confirm red.
- [ ] Implement the ordered resolver and expanded roles.
- [ ] Run the tests and confirm green.

### Task 2: Block merging and carrier renderer

**Files:** `backend/mechanism_composer.py`, `backend/mechanism_block_renderer.py`, `tests/test_mechanism_block_renderer.py`

- [ ] Add failing tests for compatible merge keys, conflicts, unresolved exclusion, sentence/bullet/numbered selection, and internal-label absence.
- [ ] Implement minimal deterministic behavior.
- [ ] Run focused tests.

### Task 3: Six-chapter audit

**Files:** `scripts/generate_phase43_semantic_samples.py`, `tests/test_phase43_reference_samples.py`, `artifacts/planning-content-phase4.3-2026-08-17/*`

- [ ] Add an integration test for all requested metrics and traceability.
- [ ] Generate separate JSON, role-audit, final Markdown, quality, and provenance artifacts.
- [ ] Run Phase 1–4.3 regression tests and verify the source job hash.
