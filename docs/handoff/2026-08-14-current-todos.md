# 新会话交接：当前待办与执行顺序

更新时间：2026-08-14（Asia/Shanghai）

## 1. 当前基线

- 仓库：`C:\Users\momoca\Documents\ai策划案\prdgenerator_decision_card`
- 分支：`codex/planner-decision-card`
- 最新已提交且已推送：`7471e45 feat: separate gameplay logic from presentation`
- 正式任务：`8312a91c89e144e6a59f81b982f14c06`
- 当前正式任务数据：`data/jobs/8312a91c89e144e6a59f81b982f14c06/job.json`
- 最近核对时玩法审核模型 revision：`365`
- 用户已确认扩展后的 `GP-TC39.1～GP-TC39.12` 验收用例，但当前任务正文迁移尚未执行。

## 2. 尚未提交、必须保留的本轮成果

以下文件属于同一批“手写策划文档逐项差异门禁”工作，严禁丢弃或被无关清理覆盖：

- `backend/feishu_language_quality.py`
- `docs/qa/prd-depth-40-acceptance-cases.md`
- `docs/qa/handwritten-vs-current-prd-gap-matrix.md`（新文件）
- `skills/feishu-gameplay-prd-reconstruction/SKILL.md`
- `skills/feishu-gameplay-prd-reconstruction/references/quality-gates.md`
- `tests/test_feishu_gameplay_prd_reconstruction_skill.py`
- `tests/test_prd_editorial_quality.py`

已完成内容：

- 完整阅读并逐项比对 3 份手写/样例材料，共 1,885 行。
- 建立 9 个维度的总体对照，以及 17 个章节/职责级差异项。
- 将 GP-TC39 扩展为 12 个验收子项。
- Skill 新增“必须逐章、逐职责、带来源定位做手写文档差异比较”的硬门禁。
- 后端新增 `HANDWRITTEN_DELTA_DIMENSIONS` 和 `handwritten_delta_matrix(...)`。
- 相关测试最新结果：`17 passed`，仅有无害的 pytest cache 权限警告。

提交前只精确暂存以上文件。工作区还有大量未跟踪测试目录、截图和运行时依赖，不得批量暂存、删除或覆盖。

## 3. 当前最重要的内容缺口

此前 P1 的“通过”结论已被更严格的 GP-TC39 门禁推翻，必须从 P1 重新验收。

1. 7 个现有章节的 `fieldDictionary` 均为空。
2. 7 个现有章节的 `formulae` 均为空。
3. 只有“载具”包含生命周期；其余 6 章缺少生命周期或明确的不适用依据。
4. 缺少独立“关卡”章节；波次、Boss、时间和胜负职责混入“怪物”。
5. 结算只细写成功分支；失败结算、中断恢复、奖励幂等、重试/返回不完整。
6. 6 张图解均缺少 `chapterId`；`GDI-101` 已过期。
7. `GTB-102`、`GTB-102B` 已过期，并混有样例项目事实。
8. 当前正文混入无本项目证据的样例事实：
   - “1 个默认武器栏 + 4 个自选武器栏”，与当前素材可见 1～6 栏冲突；
   - 特权载具、礼包 ID、活动结束重置；
   - 载具初始等级/升级规则可能来自 GVE16，必须补证据或进入决策卡。
9. 仍有 AI/审计口吻：如“由本章统一约束”“按自身攻击方式产生效果”“当前截图显示……”。
10. 正常玩法流程仍混入表现层描述，例如红色预警、Boss UI，不符合“逻辑规则与表现规则分开”。
11. 普通强化、终极强化、独立抽取复用了相似随机决策卡，缺少各自真实的候选池、结果、成本和生命周期职责。

完整依据见：`docs/qa/handwritten-vs-current-prd-gap-matrix.md`。

## 4. 下一步必须按顺序执行

### A. 先保护本轮成果

1. 核对上述 7 个文件的 diff。
2. 跑相关定向测试。
3. 精确提交并推送该批 GP-TC39 门禁变更；不要带入测试产物和运行时目录。

### B. 先写迁移测试，再改正式任务

至少覆盖：

- 样例专属事实必须删除、转为知识储备或进入决策卡，不能写成当前项目结论。
- 新增独立“关卡”章节，或为所有关卡职责建立清晰且唯一的归属。
- “怪物”不再承担 Boss 阶段切换、整局时间和胜负结算职责。
- 补齐失败结算、生命周期、中断恢复和奖励幂等；素材不足时生成可执行决策卡。
- 缺字段、配置来源或公式依据时，不能标记章节通过。
- 图解必须绑定章节/正文位置；过期图解不得参与预览或导出。

### C. 迁移当前正式任务

1. 修改 `scripts/migrate_current_job_prd_depth.py`，或建立专用迁移脚本。
2. 修改前备份正式任务 `job.json`。
3. 迁移 revision 必须高于 365。
4. 保留用户已解决的决策，不得自动回答现有 11 张待决策卡。
5. 重建动态目录、正文、图解绑定、业务表和真实完成度。

### D. 重新分阶段验收

固定顺序：验收用例已确认 → 实现 → 真实浏览器自检 → 每用例独立完整截图 → 用户验收 → 才进入下一阶段。

- 先只重做 P1，不要自行推进 P2～P7。
- P1 必须证明大机制拆分、章节职责、属性输出和样例事实隔离已经正确。
- 用户明确说“P1 验收通过”后，才能继续 P2～P7。
- 截图不得复用、不得截断、不得拿同一画面冒充多个用例。
- “大量不适用”不能自动算通过；失败项必须给出具体改善路径。

## 5. 后续整体交付待办

- 重跑完整 40 条验收用例及扩展子项。
- 逐条核对飞书反馈表；除状态为“验收无误”外，其余全部修复。
- 全链路检查 P1～P7：按钮、下一步、返回、URL、刷新恢复、对象原位、保存、滚动、缩放、失败任务进入工作台、API 配置回跳、授权与导出。
- 每一步明确展示门禁和未通过原因，避免用户反复点击却不知道为什么不能进入下一页。
- 重构当前正式任务输出，确保正文以玩法逻辑为主；表现类信息进入策划草图/竞品参考，不在正文重复。
- 最终网页预览、策划草图、竞品参考和飞书文档必须使用同一内容模型与同一排布。
- 最终导出飞书时，标题使用飞书原生有序列表/标题系统；首次发布自动授权并让用户选择保存目录，更新原文档不重复询问目录。

## 6. 不得回退的通用规则

- 不为凑字数写常识、证据审计话术或重复正文。
- 不使用固定万能标题；标题由实际机制及信息量决定，短小内容应合理合并。
- 属性不能只放表格：正文说明属性如何参与规则、何时读取、如何变化和何时重置；表格承载集中查配信息。
- 逻辑规则与表现规则分开：玩法正文写判定、状态、资源、顺序、公式、异常和生命周期；画面位置、颜色、动效、血条等进入草图/交互证据层。
- 策划草图解读出的玩法事实必须同步进入对应正文，但表现信息不得重复搬运。
- 流程图必须嵌入对应正文位置，并与前后段落共同解释机制；不能单独堆在章节外。
- 多个异质机制不能全部塞进一个大章节；也不能为显得丰富而凭空拆章。
- 决策卡仅在多种合理解释且证据不足时出现；必须含可执行选项、自己填写、暂时跳过、推荐依据和影响范围。
- 所有完成度、顶部步骤、侧边状态与导出门禁必须来自同一份真实数据。
- 测试通过不等于正文对齐；必须同时完成人工主策视角的逐章比较。

## 7. 新会话建议开场指令

> 读取 `docs/handoff/2026-08-14-current-todos.md`、项目 `.claude/memory/` 和相关 Skill。当前分支是 `codex/planner-decision-card`，不要丢弃未提交的 GP-TC39 文件，也不要批量处理未跟踪测试产物。先核对并提交 GP-TC39 手写文档差异门禁，然后按 TDD 修改当前任务 `8312a91c89e144e6a59f81b982f14c06`。先只完成 P1 重构、自检和独立截图证据，等我验收通过再继续 P2～P7。
