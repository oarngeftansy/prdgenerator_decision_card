# Use a declarative mechanic knowledge graph

Cross-project mechanic recognition uses versioned JSON nodes and edges plus one generic graph engine. Pattern definitions may declare evidence signals and responsibility activation contracts, but may not contain project answers; project structure is composed from detected patterns through shared Entity, Resource, State, Event and Flow nodes instead of an exclusive if/else classifier.
